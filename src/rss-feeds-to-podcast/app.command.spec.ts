import {AppCommand} from "./app.command";

describe('AppCommand', () => {
    it('should be defined', () => expect(AppCommand).toBeDefined())

    it('should be true', () => expect(true).toBe(true));
})